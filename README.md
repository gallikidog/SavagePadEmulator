# SavagePadEmulator v0.5.4

Fixes the visual mapper startup crash caused by `SplitContainer.SplitterDistance` being assigned while WinForms was still performing an intermediate narrow layout.

No mapping, calibration, profile, or emulation behavior was changed.
